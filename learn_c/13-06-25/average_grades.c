#include <stdio.h>

int main()
{
    //  to hold values
    float scores[10];
    // to keep track
    int index = 0;
    // to accept input values
    float input;

    printf("Enter scores (type -1 to stop):\n");

    while (1)
    {
    }
}
